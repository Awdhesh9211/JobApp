spring.application.name=JobApp
server.port=8080

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/mydb
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


## Expose actuator endpoints (choose what you want)
#management.endpoints.web.exposure.include=health,info,env,metrics,threaddump,mappings,loggers
#management.endpoint.health.show-details=always
#
## Register to Admin Server (Admin URL)
#spring.boot.admin.client.url=http://localhost:8081